import Nav from '../components/Nav';
import CustomTabs from '../components/CutomTabs';

const Home = ({ location }) => {
    return (
        <>
            <Nav/>
            <CustomTabs/>
        </>
    );
}

export default Home;